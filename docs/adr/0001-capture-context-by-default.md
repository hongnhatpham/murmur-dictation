# Capture focused context by default

Dictation correction captures the active application, window title, selection, and surrounding focused text by default, holds the Context Snapshot only for the correction, and lets the user disable capture. Password fields, password managers, private browser windows, security dialogs, and user-denied applications remain hard exclusions because correction quality does not justify transmitting secrets.
