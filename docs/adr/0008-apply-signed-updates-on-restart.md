# Apply signed updates on restart

The app checks daily, downloads Tauri-signed updates automatically, and installs a ready update on the next ordinary restart. It never restarts while recording or processing a Meeting Session, trading immediate rollout for protection against lost audio and interrupted work.
