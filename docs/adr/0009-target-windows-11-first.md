# Target Windows 11 first

V1 targets Windows 11 only and uses Tauri with a React and TypeScript interface, Rust for native integration, and SQLite plus local files for storage. Linux and other platforms are deferred because matching Windows audio capture, shortcuts, context access, insertion, credentials, packaging, and updates would double the native work before the personal workflow is proven.
